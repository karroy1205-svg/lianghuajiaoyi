// 前端配置文件
window.QUANT_CONFIG = {
    // 后端API地址，使用相对路径，自动适配当前页面的域名/IP
    API_BASE_URL: "/api",
    // WebSocket地址，使用相对路径，自动适配当前页面的域名/IP
    WS_BASE_URL: "/ws",
    // 系统名称
    APP_NAME: "A股个人自用量化投研分析系统",
    // 版本号
    VERSION: "1.0.0"
};
